<p>Faça um programa para calcular o valor de uma viagem.</p><br> 

<h4>Você terá 3 variaveis, sendo elas;</h4> <br>
<ul>
  <li> 1 - Preço do combustivel;</li><br>
<li> 2 - Gasto médio de combustivel do carro por KM;</li><br>
<li> 3 - Distancia em KM da viagem;</li><br>
</ul>

<p>Imprima no console o valor que será gasto de combustivel para realizar a viagem. 
</p>

<form id="formulario combustivel">
    <label for="precoCombustivel">Preço do Combustível por Litro (R$):</label>
    <input type="number" id="precoCombustivel" step="0.01" required><br><br>

    <label for="gastoMedioKm">Consumo por Litro (km/l):</label>
    <input type="number" id="gastoMedioKm" step="0.01" required><br><br>

    <label for="distanciaKm">Distância Percorrida (km):</label>
    <input type="number" id="distanciaKm" step="0.01" required><br><br>

    <button type="button" onclick="calculateGastoComb()">Calcular</button>
  </form>
 <p id="resultado"></p>

body{
  background: #ff50f0;
}
ul {
  font-family: arial;
  background: #fffff;  
  color: black;
}

p {
  font-family: arial;
  color: black;
  font-weight: bold;
  background: pink;
  text-align: center;
}

h4 {
  font-family: arial;
  color: black;
  font-weight: bold;
}

function calculateGastoComb(){
      const precoCombustivel = parseFloat(document.getElementById("precoCombustivel").value);                         
      const gastoMedioKm = parseFloat(document.getElementById("gastoMedioKm").value);
      const distaciaKm = parseFloat(document.getElementById("distaciaKm").value);

   if (isNaN(precoCombustivel) || isNaN(gastoMedioKm) || isNaN(distaciaKm)) {
        alert('Por favor, insira valores numéricos válidos.');
        return;
      }
  
      const gastoPorLitros = (distaciaKm / gastoMedioKm) * precoCombustivel;
document.getElementById("resultado").innerText = `Valor Gasto em Combustível: R$ ${gastoPorLitros.toFixed(2)}`;
    }
